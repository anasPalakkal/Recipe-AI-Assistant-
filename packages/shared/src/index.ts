// Barrel file for everything shared between apps/api and apps/web:
// Zod schemas, inferred types, and cross-cutting constants (error codes, etc).
// Kept intentionally minimal until the first real contract (e.g. Recipe) is
// defined in Phase 1 - nothing here is aspirational/unused.

export const SHARED_PACKAGE_PLACEHOLDER = true;
